- name : Demo on Roles
  hosts :  all
  connection : local
  roles :
    - demo

- name : print URL
  hosts : all
  connection : local
  tasks :
    - name : Print URL
      ansible.builtin.debug:
        msg: URL == {{URL}}